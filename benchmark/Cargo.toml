[package]
name = "benchmark"
version = "0.1.0"
authors = ["Matteo Santamaria"]
edition = "2024"
rust-version = "1.89"
publish = false
license = "Apache-2.0"

[dependencies]
presolve = { path = ".." }
flate2 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
